import { createList, getList, getTodos, getAllLists, getAllListsAndTodos, updateList, deleteList } from "./list";
import  { createTodo, getTodo, getAllTodos, updateTodo, deleteTodo } from "./todo";
export default {
    createList, getList, getTodos, getAllLists, getAllListsAndTodos, updateList, deleteList,
    createTodo, getTodo, getAllTodos, updateTodo, deleteTodo,
};
